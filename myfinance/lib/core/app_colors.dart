import 'package:flutter/material.dart';

class AppColors {
  // Cores principais do aplicativo (baseadas na identidade visual do MyFinance)
  static const Color primary = Color(0xFF0A3D62); // Azul profundo
  static const Color secondary = Color(0xFF60A3D9); // Azul claro
  static const Color accent = Color(0xFF26C281); // Verde financeiro

  // Cores de fundo
  static const Color backgroundLight = Color(0xFFFFFFFF);
  static const Color backgroundDark = Color(0xFF121212);

  // Cores de texto
  static const Color textLight = Color(0xFF000000);
  static const Color textDark = Color(0xFFFFFFFF);

  // Cores de erro e sucesso
  static const Color error = Color(0xFFE74C3C);
  static const Color success = Color(0xFF2ECC71);

  // Cores para botões e elementos interativos
  static const Color buttonLight = Color(0xFF3498DB);
  static const Color buttonDark = Color(0xFF2980B9);

  // Cores de sombreamento e bordas
  static const Color shadow = Color(0xFFBDC3C7);
  static const Color border = Color(0xFF95A5A6);
}
